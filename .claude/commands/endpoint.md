# Build API Endpoint / Server Action

$ARGUMENTS

## Workflow

1. **Context:** Read feature spec, `.docs/agent/architecture.md`, `.docs/agent/quality.md`
2. **Implement:** Typed inputs/outputs, input validation, structured errors, caching
3. **Tests:** Success + failure paths, edge cases
4. **Handoff:** Document API contract, propose Frontend + QA
