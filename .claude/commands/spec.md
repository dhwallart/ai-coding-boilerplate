# Feature Specification

$ARGUMENTS

## Workflow

1. **Specialization gate:** Read `.docs/project/specialization.md` -- if placeholders like `<PROJECT_DOMAIN>` exist, complete specialization first
2. **Scope:** Clarify goal, non-goals, split large requests into small deployable features
3. **Spec:** Write to `.features/<feature-name>.md` with: Goal, User Stories, Acceptance Criteria, Edge Cases, Risks
4. **Handoff:** Propose Solution Architect for architecture design
