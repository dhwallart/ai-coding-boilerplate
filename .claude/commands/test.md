# Run Tests

$ARGUMENTS

## Workflow

1. **Automated:**
   ```bash
   npm run lint && npm run build
   ```
   Run test command if configured (e.g. `npm test`, `npx vitest run`)
2. **Report:** Summary of pass/fail, failing test details
3. **Fix:** If failures found, propose fixes or handoff to relevant developer
